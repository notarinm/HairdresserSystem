<Window x:Class="HairdresserSystemWPF.MasterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Парикмахерская Стиль - Рабочее место мастера" 
        Height="500" Width="700"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2c3e50" CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <TextBlock x:Name="lblWelcome" Text="Добро пожаловать!" 
                       FontSize="18" FontWeight="Bold" Foreground="White"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Дата:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker x:Name="dpDate" Width="150" SelectedDateChanged="DpDate_SelectedDateChanged"/>
            <Button x:Name="btnRefresh" Content="Показать расписание" Width="150" Height="30" 
                    Margin="15,0,0,0" Background="#3498db" Foreground="White"
                    Click="BtnRefresh_Click"/>
            <Button x:Name="btnComplete" Content="Завершить визит" Width="130" Height="30" 
                    Margin="15,0,0,0" Background="#27ae60" Foreground="White"
                    Click="BtnComplete_Click"/>
            <Button x:Name="btnLogout" Content="Выход" Width="100" Height="30" 
                    Margin="15,0,0,0" Background="#e74c3c" Foreground="White"
                    Click="BtnLogout_Click"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#ddd" BorderThickness="1" CornerRadius="10" Padding="10"
                Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Моё расписание" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                <ListBox x:Name="lstSchedule" Grid.Row="1" 
                         DisplayMemberPath="DisplayText"
                         SelectionMode="Single"/>
            </Grid>
        </Border>
    </Grid>
</Window>
