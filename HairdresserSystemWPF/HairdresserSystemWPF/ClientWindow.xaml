<Window x:Class="HairdresserSystemWPF.ClientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Парикмахерская Стиль - Личный кабинет" 
        Height="550" Width="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Стиль для кнопок -->
        <Style TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- Стиль для элементов списка -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#dee2e6"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2c3e50" Padding="15" Margin="0,0,0,15">
            <TextBlock x:Name="lblWelcome" Text="Добро пожаловать!" 
                       FontSize="18" FontWeight="Bold" Foreground="White"/>
        </Border>

        <!-- Кнопки действий -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button x:Name="btnBook" Content="📅 Записаться онлайн" 
                    Width="160" Background="#3498db" Foreground="White"
                    Click="BtnBook_Click"/>
            <Button x:Name="btnLogout" Content="🚪 Выход" 
                    Width="100" Margin="15,0,0,0" Background="#6c757d" Foreground="White"
                    Click="BtnLogout_Click"/>
        </StackPanel>

        <!-- Список записей -->
        <Border Grid.Row="2" BorderBrush="#dee2e6" BorderThickness="1" Padding="10" Background="#f8f9fa">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="📋 Мои записи" FontSize="16" FontWeight="Bold" 
                           Foreground="#2c3e50" Margin="0,0,0,10"/>

                <ListBox x:Name="lstAppointments" Grid.Row="1" 
                         FontSize="13" SelectionMode="Single"
                         DisplayMemberPath="DisplayText"/>

                <Button x:Name="btnCancel" Grid.Row="2" Content="❌ Отменить выбранную запись" 
                        Width="220" Height="40" Margin="0,15,0,0"
                        HorizontalAlignment="Center" Background="#dc3545" Foreground="White" 
                        FontSize="13" Click="BtnCancel_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>